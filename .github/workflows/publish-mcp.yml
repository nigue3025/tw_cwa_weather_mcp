name: Publish to MCP Registry

on:
  push:
    tags: ["v*"]  # Triggers on version tags like v1.0.0

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write  # Required for OIDC authentication
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        working-directory: ./cwa_api
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        working-directory: ./cwa_api
        run: python -m build

      - name: Publish to PyPI
        working-directory: ./cwa_api
        continue-on-error: true
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: twine upload dist/* --skip-existing

      - name: Wait for PyPI to index package
        run: sleep 30

      - name: Install MCP Publisher
        run: |
          echo "Installing MCP Publisher..."
          ARCH=$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/')
          OS=$(uname -s | tr '[:upper:]' '[:lower:]')
          echo "OS: $OS, ARCH: $ARCH"
          URL="https://github.com/modelcontextprotocol/registry/releases/download/latest/mcp-publisher_${OS}_${ARCH}.tar.gz"
          echo "Downloading from: $URL"
          curl -L -f "$URL" -o mcp-publisher.tar.gz || { echo "Download failed"; exit 1; }
          echo "Extracting..."
          tar -xzf mcp-publisher.tar.gz || { echo "Extraction failed"; exit 1; }
          chmod +x mcp-publisher
          echo "Verifying installation..."
          ./mcp-publisher --version || { echo "MCP Publisher not working"; exit 1; }

      - name: Login to MCP Registry
        run: ./mcp-publisher login github-oidc

      - name: Publish to MCP Registry
        working-directory: ./cwa_api
        run: ../mcp-publisher publish
